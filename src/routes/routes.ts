export enum Routes {
    Login = "/login",
    Register = "/register",
    Dashboard = "/dashboard",
    Profile = "/profile",
    VariableInputIndex = "/variable-input",
    VariableOutputIndex = "/variable-output",
    ResultIndex = "/result",
}

export enum EndpointApi {
    Login = "/login",
    Register = "/register",
    GetUser = "/user",
}
