export enum Routes {
    Login = "/login",
    Register = "/register",
    Dashboard = "/dashboard",
}

export enum EndpointApi {
    Login = "/login",
    Register = "/register",
    GetUser = "/user",
}
